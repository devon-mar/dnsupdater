---
version: "2"
linters:
  enable:
    - bodyclose
    - copyloopvar
    - forbidigo
    - misspell
    - sloglint
  settings:
    errcheck:
      exclude-functions:
        - (*os.File).Close
formatters:
  enable:
    - gofumpt
